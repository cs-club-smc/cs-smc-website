// tan-stack router routes here
import { Home } from "../pages/home";

export const Routes = () => {
  return (
    // Router Routes
    // route to home page
    // route to about page
    // route to contact/join
    // route to members
    // route to projects
    // route to separate pages for each project
    // route to announcements
    // route to events tied to calendar
    <div>Routes</div>
  );
};
